@model a1OEC.Models.Variety

@{
    ViewData["message"] = @ViewBag.message;
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["CreateCropId"] = @ViewBag.CreateCropId;
}

<p><h2>@ViewData["message"]</h2></p>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
           <!-- <div class="form-group">
                <input type="hidden" asp-for="CropId" asp-items="ViewBag.CropId">
            </div>
               -->
            <!--
          <div class="form-group">
                <label asp-for="CropId" class="control-label"></label>
                <select asp-for="CropId" class ="form-control" asp-items="ViewBag.CropId"></select>
            </div>
                -->
            <input type="hidden" asp-for="CropId" asp-items=ViewData["CreateCropId"]>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
